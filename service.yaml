#Service Type nodePort
apiVersion: v1
kind: Service
metadata:
  name: editor-service
  labels:
    app: editor-app
spec:
  selector:
    app: editor-app
  type: LoadBalancer
  ports:
    - nodePort: 31000
      port: 8081
      targetPort: 8081
